export async function setupNotificationTables(supabase) {
  // This function is intentionally left blank as requested.
  return Promise.resolve({ success: true });
}

export default setupNotificationTables;
